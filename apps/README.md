# Apps

`apps/` contains executable entrypoints.

- `sarifc`: maintained stage-0 CLI for `format`, `check`, `run`, `build`, `doc`, and retained bootstrap bridge commands

Keep this boundary thin. CLI policy belongs here; language semantics do not.
