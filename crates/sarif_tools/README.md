# `sarif_tools`

Owns maintained tooling built on the compiler libraries.

Current surface:

- formatter
- docs renderer
- reports
- replay support library

This crate should not become a second compiler pipeline.
